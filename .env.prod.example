# 서버에서 .env 로 복사한 뒤 값을 채운다.
#   cp .env.prod.example .env
#
# .env 는 절대 git에 올리지 않는다.

DB_PASSWORD=바꿔주세요
JWT_SECRET=바꿔주세요_최소_32자_이상
JWT_ACCESS_TTL=1800000

# 검사지 OCR·요약 생성용. 앱에 박아 넣지 말 것 - 번들에서 그대로 꺼내진다.
OPENAI_API_KEY=바꿔주세요
OPENAI_DAILY_LIMIT=60
